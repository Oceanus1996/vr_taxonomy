repo,issue_id,commit_sha,filename,file_status,additions,deletions,changes,patch
aframevr/aframe,541,2a091c17ae46d929bef3e6b3401dd44553222028,src/core/a-entity.js,modified,2,0,2,"@@ -67,6 +67,7 @@ var proto = {
     value: function () {
       if (!this.parentEl || this.isScene) { return; }
       this.parentEl.remove(this);
+      this.updateComponents();
     }
   },
 
@@ -272,6 +273,7 @@ var proto = {
         // component and the component it's not defined via
         // mixins
         if (!this.isComponentDefined(name) ||
+            !this.parentNode ||
             newData === null && !isDefault && !isMixedIn) {
           component.remove();
           delete this.components[name];"
