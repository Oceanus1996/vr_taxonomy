repo,issue_id,commit_sha,filename,file_status,additions,deletions,changes,patch
brandonmousseau/vhvr-mod,57,718437134b20c82d308069faca70c0329706a954,ValheimVRMod/VRCore/UI/VRControls.cs,modified,1,19,20,"@@ -208,12 +208,6 @@ public bool GetButtonDown(string zinput)
                     return false;
                 }
             }
-            if (zinput == ""Map"" && altPieceRotationControlsActive())
-            {
-                // Disable the regular map input if alternative piece
-                // rotation controls are active.
-                return false;
-            }
             SteamVR_Action_Boolean action;
             zInputToBooleanAction.TryGetValue(zinput, out action);
             if (action == null)
@@ -234,12 +228,6 @@ public bool GetButton(string zinput)
             {
                 return false;
             }
-            if (zinput == ""Map"" && altPieceRotationControlsActive())
-            {
-                // Disable the regular map input if alternative piece
-                // rotation controls are active.
-                return false;
-            }
             SteamVR_Action_Boolean action;
             zInputToBooleanAction.TryGetValue(zinput, out action);
             if (action == null)
@@ -260,12 +248,6 @@ public bool GetButtonUp(string zinput)
             {
                 return false;
             }
-            if (zinput == ""Map"" && altPieceRotationControlsActive())
-            {
-                // Disable the regular map input if alternative piece
-                // rotation controls are active.
-                return false;
-            }
             SteamVR_Action_Boolean action;
             zInputToBooleanAction.TryGetValue(zinput, out action);
             if (action == null)
@@ -412,7 +394,7 @@ private bool altPieceRotationControlsActive()
                         inPlaceMode() &&
                         hasPlacementGhost() &&
                         !Hud.IsPieceSelectionVisible() &&
-                        SteamVR_Actions.valheim_ToggleMap.GetState(SteamVR_Input_Sources.Any);
+                        SteamVR_Actions.valheim_Grab.GetState(SteamVR_Input_Sources.LeftHand);
         }
 
         // disable Jump input under certain conditions"
