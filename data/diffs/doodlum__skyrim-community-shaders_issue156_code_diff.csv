repo,issue_id,commit_sha,filename,file_status,additions,deletions,changes,patch
doodlum/skyrim-community-shaders,156,98a6f254e72116dc1cfe1c795bcad6a0f7a8679f,src/Hooks.cpp,modified,13,2,15,"@@ -222,12 +222,23 @@ static inline REL::Relocation<decltype(hk_PollInputDevices)> _InputFunc;
 
 void hk_PollInputDevices(RE::BSTEventSource<RE::InputEvent*>* a_dispatcher, RE::InputEvent* const* a_events)
 {
+	bool blockedDevice = true;
 	auto menu = Menu::GetSingleton();
 
-	if (a_events)
+	if (a_events) {
 		menu->ProcessInputEvents(a_events);
 
-	if (menu->ShouldSwallowInput()) {  //the menu is open, eat all keypresses
+		if (*a_events) {
+			if (auto device = (*a_events)->GetDevice()) {
+				// Check that the device is not a Gamepad or VR controller. If it is, unblock input. Values 7 & 8 are returned at least for some VR Controllers.
+				int kVRRightAlt = 7;
+				int kVRLeftAlt = 8;
+				blockedDevice = !((device == RE::INPUT_DEVICES::INPUT_DEVICE::kGamepad) || (device == RE::INPUT_DEVICES::INPUT_DEVICE::kVRRight) || (device == RE::INPUT_DEVICES::INPUT_DEVICE::kVRLeft) || (device == kVRRightAlt) || (device == kVRLeftAlt));
+			}
+		}
+	}
+
+	if (blockedDevice && menu->ShouldSwallowInput()) {  //the menu is open, eat all keypresses
 		constexpr RE::InputEvent* const dummy[] = { nullptr };
 		_InputFunc(a_dispatcher, dummy);
 		return;"
doodlum/skyrim-community-shaders,156,98a6f254e72116dc1cfe1c795bcad6a0f7a8679f,src/Menu.cpp,modified,14,9,23,"@@ -542,17 +542,22 @@ void Menu::DrawOverlay()
 		}
 
 		ImGui::End();
-	} else if (failed && !hide) {
-		ImGui::SetNextWindowBgAlpha(1);
-		ImGui::SetNextWindowPos(ImVec2(10, 10));
-		if (!ImGui::Begin(""ShaderCompilationInfo"", nullptr, ImGuiWindowFlags_NoTitleBar | ImGuiWindowFlags_AlwaysAutoResize | ImGuiWindowFlags_NoResize | ImGuiWindowFlags_NoMove | ImGuiWindowFlags_NoSavedSettings)) {
+	} else if (failed) {
+		ImGui::PushStyleColor(ImGuiCol_Text, IM_COL32(255, 0, 0, 255));
+		if (!hide) {
+			ImGui::SetNextWindowBgAlpha(1);
+			ImGui::SetNextWindowPos(ImVec2(10, 10));
+			if (!ImGui::Begin(""ShaderCompilationInfo"", nullptr, ImGuiWindowFlags_NoTitleBar | ImGuiWindowFlags_AlwaysAutoResize | ImGuiWindowFlags_NoResize | ImGuiWindowFlags_NoMove | ImGuiWindowFlags_NoSavedSettings)) {
+				ImGui::End();
+				return;
+			}
+
+			ImGui::Text(""ERROR: %d shaders failed to compile. Check installation and CommunityShaders.log"", failed, totalShaders);
 			ImGui::End();
-			return;
 		}
-
-		ImGui::Text(""ERROR: %d shaders failed to compile. Check installation and CommunityShaders.log"", failed, totalShaders);
-		ImGui::PushStyleColor(ImGuiCol_Text, IM_COL32(255, 0, 0, 255));
-		ImGui::End();
+	} else {
+		// Done compiling with no failures
+		ImGui::PushStyleColor(ImGuiCol_Text, IM_COL32(255, 255, 255, 255));
 	}
 
 	if (IsEnabled) {"
