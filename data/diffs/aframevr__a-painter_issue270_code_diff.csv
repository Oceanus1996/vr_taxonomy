repo,issue_id,commit_sha,filename,file_status,additions,deletions,changes,patch
aframevr/a-painter,270,8c355f3,src/dragndrop.js,modified,32,24,56,"@@ -17,7 +17,7 @@ window.addEventListener('load', function (event) {
     for (var i = 0; i < files.length; i++) {
       var file = files[i];
 
-      if (file.name.substr(file.name.length - 4).toLowerCase() === '.apa') {
+      if (file.name.substring(file.name.length - 4).toLowerCase() === '.apa') {
         // a-painter binary
         var reader = new FileReader();
 
@@ -27,7 +27,7 @@ window.addEventListener('load', function (event) {
         };
         reader.readAsArrayBuffer(file);
       }
-      else if (file.name.substr(file.name.length - 5).toLowerCase() === '.json') {
+      else if (file.name.substring(file.name.length - 5).toLowerCase() === '.json') {
         // a-painter json
         var reader = new FileReader();
 
@@ -37,7 +37,7 @@ window.addEventListener('load', function (event) {
         };
         reader.readAsText(file);
       } 
-      else if (file.name.substr(file.name.length - 4).toLowerCase() === '.obj') {
+      else if (file.name.substring(file.name.length - 4).toLowerCase() === '.obj') {
         // OBJs
         reader = new FileReader();
 
@@ -50,7 +50,13 @@ window.addEventListener('load', function (event) {
           // set all mesh objects to dark gray
           for (var o = 0; o < mesh.children.length; o++) {
             var child = mesh.children[o];
-            child.material.color.set('#333');
+            if (child.material.constructor === Array) {
+              child.material.forEach(mat => {
+                mat.color.set('#333');
+              })
+            } else {
+              child.material.color.set('#333');
+            }
           }
           // add mesh to entity
           entity.setObject3D('mesh', mesh);
@@ -66,27 +72,29 @@ window.addEventListener('load', function (event) {
           var img = new Image();
           img.src = event.target.result;
 
-          // find good image size
-          var width, height;
-          if (img.width > img.height) {
-            width = 1.0;
-            height = img.height / img.width;
-          } else {
-            height = 1.0;
-            width = img.width / img.height;
+          img.onload = () => {
+            // find good image size
+            var width, height;
+            if (img.width > img.height) {
+              width = 1.0;
+              height = img.height / img.width;
+            } else {
+              height = 1.0;
+              width = img.width / img.height;
+            }
+  
+            // find a random position in a side of the room
+            var pos = [Math.random() * 3 - 1.5, 1 + Math.random() - 0.5, -1.4 + Math.random() * 0.2];
+  
+            // create a-image entity and set attributes
+            var entity = document.createElement('a-image');
+            entity.setAttribute('src', event.target.result);
+            entity.setAttribute('position', pos.join(' '));
+            entity.setAttribute('width', width);
+            entity.setAttribute('height', height);
+            entity.className = 'templateitem';
+            document.querySelector('a-scene').appendChild(entity);
           }
-
-          // find a random position in a side of the room
-          var pos = [Math.random() * 3 - 1.5, 1 + Math.random() - 0.5, -1.4 + Math.random() * 0.2];
-
-          // create a-image entity and set attributes
-          var entity = document.createElement('a-image');
-          entity.setAttribute('src', event.target.result);
-          entity.setAttribute('position', pos.join(' '));
-          entity.setAttribute('width', width);
-          entity.setAttribute('height', height);
-          entity.className = 'templateitem';
-          document.querySelector('a-scene').appendChild(entity);
         };
         reader.readAsDataURL(file);
       }"
