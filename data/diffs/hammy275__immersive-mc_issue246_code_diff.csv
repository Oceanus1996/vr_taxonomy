repo,issue_id,commit_sha,filename,file_status,additions,deletions,changes,patch
hammy275/immersive-mc,246,96a97cd901bfc1401c7e15cf8450900b6e915dc5,common/src/main/java/com/hammy275/immersivemc/client/immersive/BuiltImmersiveImpl.java,modified,8,2,10,"@@ -258,8 +258,14 @@ private boolean airCheck(BuiltImmersiveInfo<E> infoIn) {
             }
         }
         for (BlockPos pos : positions) {
-            if (!Minecraft.getInstance().level.getBlockState(pos).canBeReplaced()) {
-                return false;
+            BlockState state = Minecraft.getInstance().level.getBlockState(pos);
+            if (state.canBeReplaced()) {
+                continue; // Replaceable blocks never block immersives.
+            }
+            AABB shape = state.getShape(Minecraft.getInstance().level, pos).bounds();
+            double volume = shape.getXsize() * shape.getYsize() * shape.getZsize();
+            if (volume > 1d/3d) {
+                return false; // Blocks with bounding boxes larger than 1/3 of a block cause blocking.
             }
         }
         return true;"
