repo,issue_id,commit_sha,filename,file_status,additions,deletions,changes,patch
webaverse/app,2446,cfbeef73298c0ef5144ae96337acf0af8e3da7e4,src/components/play-mode/hotbar/Hotbar.jsx,modified,4,4,8,"@@ -23,10 +23,10 @@ const HotbarItem = props => {
     }, [canvasRef]);
     useEffect(() => {
       function selectedchange(e) {
-        // console.log('selectedchange hotbar 1', e.data);
-        const {index} = e.data;
-        setSelected(index === props.index);
-        // console.log('selectedchange hotbar 2', e.data);
+        const {index, app} = e.data;
+        if (index === -1 || app) {
+          setSelected(index === props.index);
+        }
       }
 
       loadoutManager.addEventListener('selectedchange', selectedchange);"
webaverse/app,2446,cfbeef73298c0ef5144ae96337acf0af8e3da7e4,src/components/play-mode/infobox/Infobox.jsx,modified,4,2,6,"@@ -36,8 +36,10 @@ export const Infobox = () => {
     }, [canvasRef]);
     useEffect(() => {
         function selectedchange(e) {
-            const {app} = e.data;
-            setSelectedApp(app);
+            const {index, app} = e.data;
+            if (index === -1 || app) {
+                setSelectedApp(app);
+            }
         }
         loadoutManager.addEventListener('selectedchange', selectedchange);
         return () => {"
