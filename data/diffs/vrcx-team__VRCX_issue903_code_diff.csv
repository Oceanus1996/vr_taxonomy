repo,issue_id,commit_sha,filename,file_status,additions,deletions,changes,patch,gpt_bug_detection
vrcx-team/VRCX,903,3ccc6bbdf46a07b56bd4acdcdf3d13f1355ce837,html/src/app.js,modified,6,2,8,"@@ -991,10 +991,14 @@ speechSynthesis.getVoices();
                 }
             },
             showGroupDialog() {
-                if (!this.location || !this.link) {
+                var location = this.location;
+                if (this.isTraveling) {
+                    location = this.traveling;
+                }
+                if (!location || !this.link) {
                     return;
                 }
-                var L = API.parseLocation(this.location);
+                var L = API.parseLocation(location);
                 if (!L.groupId) {
                     return;
                 }","{""error"": ""code_file_not_found""}"
