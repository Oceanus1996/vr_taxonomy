repo,issue_id,commit_sha,filename,file_status,additions,deletions,changes,patch
supermedium/aframe-react,85,00dc94c,src/index.js,modified,27,6,33,"@@ -1,5 +1,8 @@
 import React from 'react';
 
+const nonEntityPropNames = ['children', 'events', 'primitive'];
+const filterNonEntityPropNames = propName => nonEntityPropNames.indexOf(propName) === -1;
+
 const options = {
   // React needs this because React serializes.
   // Preact does not because Preact runs `.setAttribute` on its own.
@@ -24,11 +27,29 @@ function doSetAttribute (el, props, propName) {
  * Batch `.setAttribute()`s, filtering out props not relevant to A-Frame.
  */
 function doSetAttributes (el, props) {
+}
+
+/**
+ * Handle diffing of previous and current attributes.
+ *
+ * @param {Element} el
+ * @param {Object|null} prevProps - Previous props map.
+ * @param {Object} props - Current props map.
+ */
+function updateAttributes (el, prevProps, props) {
+  if (!props || prevProps === props) { return; }
+
   // Set attributes.
-  const nonEntityPropNames = ['children', 'events', 'primitive'];
-  Object.keys(props).filter(
-    propName => nonEntityPropNames.indexOf(propName) === -1
-  ).forEach(propName => { doSetAttribute(el, props, propName); });
+  Object.keys(props).filter(filterNonEntityPropNames).forEach(propName => {
+    doSetAttribute(el, props, propName);
+  });
+
+  // See if attributes were removed.
+  if (prevProps) {
+    Object.keys(prevProps).filter(filterNonEntityPropNames).forEach(propName => {
+      if (props[propName] === undefined) { el.removeAttribute(propName); }
+    });
+  }
 }
 
 /**
@@ -55,7 +76,7 @@ export class Entity extends React.Component {
     }
 
     // Update entity.
-    doSetAttributes(el, props);
+    updateAttributes(el, null, props);
 
     // Allow ref.
     if (props._ref) { props._ref(el); }
@@ -73,7 +94,7 @@ export class Entity extends React.Component {
 
     // Update entity.
     if (options.runSetAttributeOnUpdates) {
-      doSetAttributes(el, props);
+      updateAttributes(el, prevProps, props);
     }
   }
 "
supermedium/aframe-react,85,00dc94c,tests/browser/index.test.js,modified,22,0,22,"@@ -328,6 +328,28 @@ suite('<Entity events/>', () => {
       });
     });
   });
+
+  test('can remove attribute', function (done) {
+    ReactDOM.render(
+      <Scene>
+        <Entity light={{ type: ""ambient"", color: ""#CCC"" }} />
+      </Scene>,
+      div
+    );
+    ReactDOM.render(
+      <Scene>
+        <Entity />
+      </Scene>,
+      div
+    );
+    const scene = div.querySelector('a-scene');
+    scene.addEventListener('loaded', () => {
+      setTimeout(() => {
+        assert.equal(div.querySelector('a-entity').hasAttribute('light'), false);
+        done();
+      });
+    });
+  });
 });
 
 suite('<Scene/>', () => {"
supermedium/aframe-react,85,10e497a,src/index.js,modified,27,6,33,"@@ -1,5 +1,8 @@
 import React from 'react';
 
+const nonEntityPropNames = ['children', 'events', 'primitive'];
+const filterNonEntityPropNames = propName => nonEntityPropNames.indexOf(propName) === -1;
+
 const options = {
   // React needs this because React serializes.
   // Preact does not because Preact runs `.setAttribute` on its own.
@@ -24,11 +27,29 @@ function doSetAttribute (el, props, propName) {
  * Batch `.setAttribute()`s, filtering out props not relevant to A-Frame.
  */
 function doSetAttributes (el, props) {
+}
+
+/**
+ * Handle diffing of previous and current attributes.
+ *
+ * @param {Element} el
+ * @param {Object|null} prevProps - Previous props map.
+ * @param {Object} props - Current props map.
+ */
+function updateAttributes (el, prevProps, props) {
+  if (!props || prevProps === props) { return; }
+
   // Set attributes.
-  const nonEntityPropNames = ['children', 'events', 'primitive'];
-  Object.keys(props).filter(
-    propName => nonEntityPropNames.indexOf(propName) === -1
-  ).forEach(propName => { doSetAttribute(el, props, propName); });
+  Object.keys(props).filter(filterNonEntityPropNames).forEach(propName => {
+    doSetAttribute(el, props, propName);
+  });
+
+  // See if attributes were removed.
+  if (prevProps) {
+    Object.keys(prevProps).filter(filterNonEntityPropNames).forEach(propName => {
+      if (props[propName] === undefined) { el.removeAttribute(propName); }
+    });
+  }
 }
 
 /**
@@ -55,7 +76,7 @@ export class Entity extends React.Component {
     }
 
     // Update entity.
-    doSetAttributes(el, props);
+    updateAttributes(el, null, props);
 
     // Allow ref.
     if (props._ref) { props._ref(el); }
@@ -73,7 +94,7 @@ export class Entity extends React.Component {
 
     // Update entity.
     if (options.runSetAttributeOnUpdates) {
-      doSetAttributes(el, props);
+      updateAttributes(el, prevProps, props);
     }
   }
 "
supermedium/aframe-react,85,10e497a,tests/browser/index.test.js,modified,22,0,22,"@@ -328,6 +328,28 @@ suite('<Entity events/>', () => {
       });
     });
   });
+
+  test('can remove attribute', function (done) {
+    ReactDOM.render(
+      <Scene>
+        <Entity light={{ type: ""ambient"", color: ""#CCC"" }} />
+      </Scene>,
+      div
+    );
+    ReactDOM.render(
+      <Scene>
+        <Entity />
+      </Scene>,
+      div
+    );
+    const scene = div.querySelector('a-scene');
+    scene.addEventListener('loaded', () => {
+      setTimeout(() => {
+        assert.equal(div.querySelector('a-entity').hasAttribute('light'), false);
+        done();
+      });
+    });
+  });
 });
 
 suite('<Scene/>', () => {"
supermedium/aframe-react,85,850b6b4,src/index.js,modified,27,6,33,"@@ -1,5 +1,8 @@
 import React from 'react';
 
+const nonEntityPropNames = ['children', 'events', 'primitive'];
+const filterNonEntityPropNames = propName => nonEntityPropNames.indexOf(propName) === -1;
+
 const options = {
   // React needs this because React serializes.
   // Preact does not because Preact runs `.setAttribute` on its own.
@@ -24,11 +27,29 @@ function doSetAttribute (el, props, propName) {
  * Batch `.setAttribute()`s, filtering out props not relevant to A-Frame.
  */
 function doSetAttributes (el, props) {
+}
+
+/**
+ * Handle diffing of previous and current attributes.
+ *
+ * @param {Element} el
+ * @param {Object|null} prevProps - Previous props map.
+ * @param {Object} props - Current props map.
+ */
+function updateAttributes (el, prevProps, props) {
+  if (!props || prevProps === props) { return; }
+
   // Set attributes.
-  const nonEntityPropNames = ['children', 'events', 'primitive'];
-  Object.keys(props).filter(
-    propName => nonEntityPropNames.indexOf(propName) === -1
-  ).forEach(propName => { doSetAttribute(el, props, propName); });
+  Object.keys(props).filter(filterNonEntityPropNames).forEach(propName => {
+    doSetAttribute(el, props, propName);
+  });
+
+  // See if attributes were removed.
+  if (prevProps) {
+    Object.keys(prevProps).filter(filterNonEntityPropNames).forEach(propName => {
+      if (props[propName] === undefined) { el.removeAttribute(propName); }
+    });
+  }
 }
 
 /**
@@ -55,7 +76,7 @@ export class Entity extends React.Component {
     }
 
     // Update entity.
-    doSetAttributes(el, props);
+    updateAttributes(el, null, props);
 
     // Allow ref.
     if (props._ref) { props._ref(el); }
@@ -73,7 +94,7 @@ export class Entity extends React.Component {
 
     // Update entity.
     if (options.runSetAttributeOnUpdates) {
-      doSetAttributes(el, props);
+      updateAttributes(el, prevProps, props);
     }
   }
 "
supermedium/aframe-react,85,850b6b4,tests/browser/index.test.js,modified,22,0,22,"@@ -328,6 +328,28 @@ suite('<Entity events/>', () => {
       });
     });
   });
+
+  test('can remove attribute', function (done) {
+    ReactDOM.render(
+      <Scene>
+        <Entity light={{ type: ""ambient"", color: ""#CCC"" }} />
+      </Scene>,
+      div
+    );
+    ReactDOM.render(
+      <Scene>
+        <Entity />
+      </Scene>,
+      div
+    );
+    const scene = div.querySelector('a-scene');
+    scene.addEventListener('loaded', () => {
+      setTimeout(() => {
+        assert.equal(div.querySelector('a-entity').hasAttribute('light'), false);
+        done();
+      });
+    });
+  });
 });
 
 suite('<Scene/>', () => {"
