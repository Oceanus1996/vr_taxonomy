repo,issue_id,commit_sha,filename,file_status,additions,deletions,changes,patch
ExtendRealityLtd/VRTK,260,10b8588,Assets/SteamVR_Unity_Toolkit/Scripts/VRTK_InteractableObject.cs,modified,17,0,17,"@@ -320,6 +320,14 @@ public void SetGrabbedSnapHandle(Transform handle)
             grabbedSnapHandle = handle;
         }
 
+        public void RegisterTeleporters()
+        {
+            foreach (var teleporter in GameObject.FindObjectsOfType<VRTK_BasicTeleport>())
+            {
+                teleporter.Teleported += new TeleportEventHandler(OnTeleported);
+            }
+        }
+
         protected virtual void Awake()
         {
             rb = this.GetComponent<Rigidbody>();
@@ -336,6 +344,7 @@ protected virtual void Awake()
         protected virtual void Start()
         {
             originalObjectColours = StoreOriginalColors();
+            RegisterTeleporters();
         }
 
         protected virtual void Update()
@@ -529,5 +538,13 @@ private void FixedUpdateTrackedObject()
                 rb.velocity = Vector3.MoveTowards(rb.velocity, velocityTarget, maxDistanceDelta);
             }
         }
+
+        private void OnTeleported(object sender, DestinationMarkerEventArgs e)
+        {
+            if (grabAttachMechanic == GrabAttachType.Track_Object && trackPoint)
+            {
+                this.transform.position = grabbingObject.transform.position;
+            }
+        }
     }
 }
\ No newline at end of file"
ExtendRealityLtd/VRTK,260,96de76b,Assets/SteamVR_Unity_Toolkit/Scripts/VRTK_InteractableObject.cs,modified,17,0,17,"@@ -320,6 +320,14 @@ public void SetGrabbedSnapHandle(Transform handle)
             grabbedSnapHandle = handle;
         }
 
+        public void RegisterTeleporters()
+        {
+            foreach (var teleporter in GameObject.FindObjectsOfType<VRTK_BasicTeleport>())
+            {
+                teleporter.Teleported += new TeleportEventHandler(OnTeleported);
+            }
+        }
+
         protected virtual void Awake()
         {
             rb = this.GetComponent<Rigidbody>();
@@ -336,6 +344,7 @@ protected virtual void Awake()
         protected virtual void Start()
         {
             originalObjectColours = StoreOriginalColors();
+            RegisterTeleporters();
         }
 
         protected virtual void Update()
@@ -529,5 +538,13 @@ private void FixedUpdateTrackedObject()
                 rb.velocity = Vector3.MoveTowards(rb.velocity, velocityTarget, maxDistanceDelta);
             }
         }
+
+        private void OnTeleported(object sender, DestinationMarkerEventArgs e)
+        {
+            if (grabAttachMechanic == GrabAttachType.Track_Object && trackPoint)
+            {
+                this.transform.position = grabbingObject.transform.position;
+            }
+        }
     }
 }
\ No newline at end of file"
ExtendRealityLtd/VRTK,260,3196b86,Assets/SteamVR_Unity_Toolkit/Scripts/VRTK_InteractableObject.cs,modified,17,0,17,"@@ -320,6 +320,14 @@ public void SetGrabbedSnapHandle(Transform handle)
             grabbedSnapHandle = handle;
         }
 
+        public void RegisterTeleporters()
+        {
+            foreach (var teleporter in GameObject.FindObjectsOfType<VRTK_BasicTeleport>())
+            {
+                teleporter.Teleported += new TeleportEventHandler(OnTeleported);
+            }
+        }
+
         protected virtual void Awake()
         {
             rb = this.GetComponent<Rigidbody>();
@@ -336,6 +344,7 @@ protected virtual void Awake()
         protected virtual void Start()
         {
             originalObjectColours = StoreOriginalColors();
+            RegisterTeleporters();
         }
 
         protected virtual void Update()
@@ -529,5 +538,13 @@ private void FixedUpdateTrackedObject()
                 rb.velocity = Vector3.MoveTowards(rb.velocity, velocityTarget, maxDistanceDelta);
             }
         }
+
+        private void OnTeleported(object sender, DestinationMarkerEventArgs e)
+        {
+            if (grabAttachMechanic == GrabAttachType.Track_Object && trackPoint)
+            {
+                this.transform.position = grabbingObject.transform.position;
+            }
+        }
     }
 }
\ No newline at end of file"
ExtendRealityLtd/VRTK,260,4c52adc,Assets/SteamVR_Unity_Toolkit/Scripts/VRTK_InteractableObject.cs,modified,17,0,17,"@@ -320,6 +320,14 @@ public void SetGrabbedSnapHandle(Transform handle)
             grabbedSnapHandle = handle;
         }
 
+        public void RegisterTeleporters()
+        {
+            foreach (var teleporter in GameObject.FindObjectsOfType<VRTK_BasicTeleport>())
+            {
+                teleporter.Teleported += new TeleportEventHandler(OnTeleported);
+            }
+        }
+
         protected virtual void Awake()
         {
             rb = this.GetComponent<Rigidbody>();
@@ -336,6 +344,7 @@ protected virtual void Awake()
         protected virtual void Start()
         {
             originalObjectColours = StoreOriginalColors();
+            RegisterTeleporters();
         }
 
         protected virtual void Update()
@@ -529,5 +538,13 @@ private void FixedUpdateTrackedObject()
                 rb.velocity = Vector3.MoveTowards(rb.velocity, velocityTarget, maxDistanceDelta);
             }
         }
+
+        private void OnTeleported(object sender, DestinationMarkerEventArgs e)
+        {
+            if (grabAttachMechanic == GrabAttachType.Track_Object && trackPoint)
+            {
+                this.transform.position = grabbingObject.transform.position;
+            }
+        }
     }
 }
\ No newline at end of file"
