repo,issue_id,commit_sha,filename,file_status,additions,deletions,changes,patch
vizorvr/patches,1265,fc84b24,browser/vendor/three/webvr-manager.js,modified,5,0,5,"@@ -296,6 +296,11 @@ function CardboardDistorter(renderer) {
 
     renderer.setSize = function (width, height) {
       genuineSetSize.call(renderer, width, height);
+
+      if (textureTarget && textureTarget.width == renderer.context.canvas.width && textureTarget.height == renderer.context.canvas.height) {
+        return
+      }
+
       textureTarget = createRenderTarget(renderer);
     };
   }"
vizorvr/patches,1265,3984de6,browser/vendor/three/webvr-manager.js,modified,5,0,5,"@@ -296,6 +296,11 @@ function CardboardDistorter(renderer) {
 
     renderer.setSize = function (width, height) {
       genuineSetSize.call(renderer, width, height);
+
+      if (textureTarget && textureTarget.width == renderer.context.canvas.width && textureTarget.height == renderer.context.canvas.height) {
+        return
+      }
+
       textureTarget = createRenderTarget(renderer);
     };
   }"
