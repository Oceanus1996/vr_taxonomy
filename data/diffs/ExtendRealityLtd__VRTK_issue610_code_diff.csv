repo,issue_id,commit_sha,filename,file_status,additions,deletions,changes,patch
ExtendRealityLtd/VRTK,610,d5a49df,Assets/VRTK/Scripts/Controls/2D/RadialMenuController.cs,modified,5,1,6,"@@ -10,6 +10,7 @@ public class RadialMenuController : MonoBehaviour
 
         protected RadialMenu menu;
         private float currentAngle; //Keep track of angle for when we click
+        private bool touchpadTouched;
 
         private void Awake()
         {
@@ -108,18 +109,21 @@ private void DoTouchpadUnclicked(object sender, ControllerInteractionEventArgs e
 
         private void DoTouchpadTouched(object sender, ControllerInteractionEventArgs e)
         {
+            touchpadTouched = true;
             DoShowMenu(CalculateAngle(e));
         }
 
         private void DoTouchpadUntouched(object sender, ControllerInteractionEventArgs e)
         {
+            touchpadTouched = false;
             DoHideMenu(false);
         }
 
         //Touchpad finger moved position
         private void DoTouchpadAxisChanged(object sender, ControllerInteractionEventArgs e)
         {
-            DoChangeAngle(CalculateAngle(e));
+            if (touchpadTouched)
+                DoChangeAngle(CalculateAngle(e));
         }
 
         #endregion Private Controller Listeners"
ExtendRealityLtd/VRTK,610,b26f9a5,Assets/VRTK/Scripts/Controls/2D/RadialMenuController.cs,modified,7,1,8,"@@ -10,6 +10,7 @@ public class RadialMenuController : MonoBehaviour
 
         protected RadialMenu menu;
         private float currentAngle; //Keep track of angle for when we click
+        private bool touchpadTouched;
 
         private void Awake()
         {
@@ -108,18 +109,23 @@ private void DoTouchpadUnclicked(object sender, ControllerInteractionEventArgs e
 
         private void DoTouchpadTouched(object sender, ControllerInteractionEventArgs e)
         {
+            touchpadTouched = true;
             DoShowMenu(CalculateAngle(e));
         }
 
         private void DoTouchpadUntouched(object sender, ControllerInteractionEventArgs e)
         {
+            touchpadTouched = false;
             DoHideMenu(false);
         }
 
         //Touchpad finger moved position
         private void DoTouchpadAxisChanged(object sender, ControllerInteractionEventArgs e)
         {
-            DoChangeAngle(CalculateAngle(e));
+            if (touchpadTouched)
+            {
+                DoChangeAngle(CalculateAngle(e));
+            }
         }
 
         #endregion Private Controller Listeners"
ExtendRealityLtd/VRTK,610,b155c6e,Assets/VRTK/Scripts/Controls/2D/RadialMenuController.cs,modified,7,1,8,"@@ -10,6 +10,7 @@ public class RadialMenuController : MonoBehaviour
 
         protected RadialMenu menu;
         private float currentAngle; //Keep track of angle for when we click
+        private bool touchpadTouched;
 
         private void Awake()
         {
@@ -108,18 +109,23 @@ private void DoTouchpadUnclicked(object sender, ControllerInteractionEventArgs e
 
         private void DoTouchpadTouched(object sender, ControllerInteractionEventArgs e)
         {
+            touchpadTouched = true;
             DoShowMenu(CalculateAngle(e));
         }
 
         private void DoTouchpadUntouched(object sender, ControllerInteractionEventArgs e)
         {
+            touchpadTouched = false;
             DoHideMenu(false);
         }
 
         //Touchpad finger moved position
         private void DoTouchpadAxisChanged(object sender, ControllerInteractionEventArgs e)
         {
-            DoChangeAngle(CalculateAngle(e));
+            if (touchpadTouched)
+            {
+                DoChangeAngle(CalculateAngle(e));
+            }
         }
 
         #endregion Private Controller Listeners"
ExtendRealityLtd/VRTK,610,0578788,Assets/VRTK/Scripts/Controls/2D/RadialMenuController.cs,modified,7,1,8,"@@ -10,6 +10,7 @@ public class RadialMenuController : MonoBehaviour
 
         protected RadialMenu menu;
         private float currentAngle; //Keep track of angle for when we click
+        private bool touchpadTouched;
 
         private void Awake()
         {
@@ -108,18 +109,23 @@ private void DoTouchpadUnclicked(object sender, ControllerInteractionEventArgs e
 
         private void DoTouchpadTouched(object sender, ControllerInteractionEventArgs e)
         {
+            touchpadTouched = true;
             DoShowMenu(CalculateAngle(e));
         }
 
         private void DoTouchpadUntouched(object sender, ControllerInteractionEventArgs e)
         {
+            touchpadTouched = false;
             DoHideMenu(false);
         }
 
         //Touchpad finger moved position
         private void DoTouchpadAxisChanged(object sender, ControllerInteractionEventArgs e)
         {
-            DoChangeAngle(CalculateAngle(e));
+            if (touchpadTouched)
+            {
+                DoChangeAngle(CalculateAngle(e));
+            }
         }
 
         #endregion Private Controller Listeners"
