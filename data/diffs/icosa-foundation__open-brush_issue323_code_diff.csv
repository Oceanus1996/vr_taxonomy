repo,issue_id,commit_sha,filename,file_status,additions,deletions,changes,patch
icosa-foundation/open-brush,323,3dd297f,Assets/Scripts/Layers/LayerUI_Manager.cs,modified,1,1,2,"@@ -28,7 +28,6 @@ public class LayerUI_Manager : MonoBehaviour
 
         private void Start()
         {
-            m_Canvases = new List<CanvasScript>();
             ResetUI();
         }
 
@@ -64,6 +63,7 @@ private void OnLayerCanvasesUpdate()
         // Subscribes to events
         private void OnEnable()
         {
+            ResetUI();
             App.Scene.ActiveCanvasChanged += ActiveSceneChanged;
             App.Scene.LayerCanvasesUpdate += OnLayerCanvasesUpdate;
         }"
icosa-foundation/open-brush,323,b61f6bb,Assets/Scripts/Layers/LayerUI_Manager.cs,modified,1,1,2,"@@ -28,6 +28,7 @@ public class LayerUI_Manager : MonoBehaviour
 
         private void Start()
         {
+            m_Canvases = new List<CanvasScript>();
             ResetUI();
         }
 
@@ -63,7 +64,6 @@ private void OnLayerCanvasesUpdate()
         // Subscribes to events
         private void OnEnable()
         {
-            ResetUI();
             App.Scene.ActiveCanvasChanged += ActiveSceneChanged;
             App.Scene.LayerCanvasesUpdate += OnLayerCanvasesUpdate;
         }"
