repo,issue_id,commit_sha,filename,file_status,additions,deletions,changes,patch
vrm-c/UniVRM,603,68328edd15cfb9dde90589432361cfc34651afeb,Assets/MeshUtility/Runtime/BoneNormalizer.cs,modified,8,1,9,"@@ -237,9 +237,9 @@ static void NormalizeSkinnedMesh(Transform src, Transform dst, Dictionary<Transf
             var srcMesh = srcRenderer.sharedMesh;
             var originalSrcMesh = srcMesh;
 
-            // clear blendShape
             if (clearBlendShape)
             {
+                // clear blendShape if not bake current blendshape
                 for (int i = 0; i < srcMesh.blendShapeCount; ++i)
                 {
                     srcRenderer.SetBlendShapeWeight(i, 0);
@@ -305,6 +305,13 @@ static void NormalizeSkinnedMesh(Transform src, Transform dst, Dictionary<Transf
             //
             // BlendShapes
             //
+
+            // clear blendShape always
+            for (int i = 0; i < srcMesh.blendShapeCount; ++i)
+            {
+                srcRenderer.SetBlendShapeWeight(i, 0);
+            }
+
             var meshVertices = mesh.vertices;
             var meshNormals = mesh.normals;
 #if VRM_NORMALIZE_BLENDSHAPE_TANGENT"
