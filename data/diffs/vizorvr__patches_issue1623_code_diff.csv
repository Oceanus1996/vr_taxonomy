repo,issue_id,commit_sha,filename,file_status,additions,deletions,changes,patch
vizorvr/patches,1623,0e71c66,browser/scripts/preset-manager.js,modified,4,3,7,"@@ -1,6 +1,5 @@
 
-function PresetManager(base_url)
-{
+function PresetManager(base_url) {
 	EventEmitter.call(this)
 
 	this._base_url = base_url
@@ -154,7 +153,9 @@ PresetManager.prototype.openPreset = function(name) {
 			name: name
 		})
 
-		var doc = E2.app.fillCopyBuffer(data.root.nodes, data.root.conns, 0, 0)
+		var preset = data.root ? data.root : data
+
+		var doc = E2.app.fillCopyBuffer(preset.nodes, preset.conns, 0, 0)
 		E2.app.onPaste(doc)
 	})
 	.fail(function(_j, _textStatus, _errorThrown) {"
vizorvr/patches,1623,ae16a48,browser/scripts/preset-manager.js,modified,4,3,7,"@@ -1,6 +1,5 @@
 
-function PresetManager(base_url)
-{
+function PresetManager(base_url) {
 	EventEmitter.call(this)
 
 	this._base_url = base_url
@@ -154,7 +153,9 @@ PresetManager.prototype.openPreset = function(name) {
 			name: name
 		})
 
-		var doc = E2.app.fillCopyBuffer(data.root.nodes, data.root.conns, 0, 0)
+		var preset = data.root ? data.root : data
+
+		var doc = E2.app.fillCopyBuffer(preset.nodes, preset.conns, 0, 0)
 		E2.app.onPaste(doc)
 	})
 	.fail(function(_j, _textStatus, _errorThrown) {"
